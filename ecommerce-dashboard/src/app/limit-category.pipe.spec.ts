import { LimitCategoryPipe } from './limit-category.pipe';

describe('LimitCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
